// Copy to config.js (or run ../sync-ext.sh to generate it from ../blog/.env).
// config.js is gitignored — it holds the API token baked into the app build.
const BLINKS_CONFIG = {
  server: "https://bobbby.online",
  token: "dev-blinks-token",
};
